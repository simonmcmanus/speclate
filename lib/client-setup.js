 function(pageName, context, page) {
     console.log('page', pageName);

        //sizlate.render($('#container'), );


        // $.get('/pages/' + pageName + '/' + pageName + '.html', function(markup) {
        //
        //     $('#container').html(markup);
        // });

    // check if the page type has changed.

    // Object.keys(page.spec).forEach(function(i){
    //     // get html.
    //
    //     // get data.
    //
    //     $(i).html('hi');
    //     console.log(i);
    // });

//    state.page = pageName;

};
